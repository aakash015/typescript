//now in the below mentioned case we need to 
//have types of arguments to be defined else it will be problematic 
//because if we pass string than in that case it will produce wrong result 
function addTwo(a) {
    a.toUppercase();
    return a + 2;
}
addTwo(5);
